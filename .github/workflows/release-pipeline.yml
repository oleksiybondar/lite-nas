name: Release pipeline

on:
  workflow_run:
    workflows:
      - Main pipeline
    types:
      - completed
    branches:
      - main

permissions:
  contents: read

jobs:
  release-pipeline-stub:
    name: Release pipeline stub
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Placeholder
        run: |
          echo "Main pipeline passed."
          echo "Future release packaging, publishing, or deployment steps will run here."
