name: Setup Node job
description: Install the default Node.js toolchain for CI jobs.

runs:
  using: composite
  steps:
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: 22
