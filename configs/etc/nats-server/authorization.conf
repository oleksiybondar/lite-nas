authorization {
  users: [
    {
      user: "system-metrics-svc"
      permissions: {
        publish: ["system.metrics.>"]
        subscribe: ["system.metrics.>", "_INBOX.>"]
      }
    },
    {
      user: "system-metrics-cli"
      permissions: {
        publish: ["system.metrics.>", "_INBOX.>"]
        subscribe: ["system.metrics.>", "_INBOX.>"]
      }
    }
  ]
}
