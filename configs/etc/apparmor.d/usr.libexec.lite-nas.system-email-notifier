abi <abi/4.0>,

include <tunables/global>

profile /usr/libexec/lite-nas/system-email-notifier flags=(attach_disconnected,mediate_deleted) {
  include <abstractions/lite-nas-go-base>
  include <abstractions/lite-nas-nats-client>

  /usr/libexec/lite-nas/system-email-notifier mr,
  /etc/lite-nas/system-email-notifier.conf r,
  /var/log/lite-nas/system-email-notifier.log rw,

  include if exists <local/usr.libexec.lite-nas.system-email-notifier>
}

