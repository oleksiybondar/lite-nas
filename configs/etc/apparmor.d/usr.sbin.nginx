abi <abi/4.0>,

include <tunables/global>

profile /usr/sbin/nginx flags=(attach_disconnected,mediate_deleted) {
  include <abstractions/base>
  include <abstractions/nameservice>
  include <abstractions/openssl>
  include <abstractions/ssl_certs>

  capability net_bind_service,

  network inet stream,
  network inet6 stream,
  network unix stream,
  network unix dgram,

  /usr/sbin/nginx mr,
  /usr/lib/nginx/modules/*.so mr,
  /etc/nginx/ r,
  /etc/nginx/** r,
  /etc/lite-nas/certificates/nginx/server.crt r,
  /etc/lite-nas/certificates/nginx/server.key r,
  /run/nginx.pid rwk,
  /run/nginx/** rwk,
  /var/lib/nginx/ rw,
  /var/lib/nginx/** rwk,
  /var/log/nginx/ rw,
  /var/log/nginx/** rwk,
  /usr/share/nginx/ r,
  /usr/share/nginx/** r,
  /usr/share/lite-nas/web-gateway/ r,
  /usr/share/lite-nas/web-gateway/** r,

  include if exists <local/usr.sbin.nginx>
}
